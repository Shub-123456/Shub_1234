pipeline {
    agent any
    stages {
        stage('test AWS credentials') {
            steps {
                withAWS(credentials: 'jens', region: 'ap-south-1') 
                {
                    sh 'python pipeline4/ultrafile.py'
                }
            }
        }
    }
}
